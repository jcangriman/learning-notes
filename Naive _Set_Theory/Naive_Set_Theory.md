Depedencies: 
- [Propositional Reasoning](../Propositional_Reasoning/Propositional_Reasoning.md)

Concepts:
12. **Sets & Membership** — what a set is, the $\in$ notation, set builder notation
13. **Subsets** — when every element of one set is contained in another, $\subseteq$
14. **Power Sets** — the set of all subsets of a set, $\mathcal{P}(A)$
15. **Union, Intersection, Complement** — combining and comparing sets, $\cup$, $\cap$, $\setminus$
16. **Cartesian Product** — ordered pairs and the product of two sets, $A \times B$
17. **Relations** — subsets of a Cartesian product, properties of relations
18. **Functions** — relations that assign exactly one output to each input